# 验收遗留问题清单-2026-04-26

## 1. 范围

| 项目 | 内容 |
| --- | --- |
| 对应 WBS | `14.6.1` 分类整理验收遗留问题 |
| 来源 | `功能验收记录-2026-04-26.md`、`非功能验收记录-2026-04-26.md`、本轮实际复跑结果 |
| 目标 | 分类沉淀不阻断本轮验收结论、但必须继续整改的问题 |

## 2. 遗留问题清单

| 编号 | 分类 | 问题描述 | 影响范围 | 当前状态 | 责任角色 |
| --- | --- | --- | --- | --- | --- |
| R-01 | 环境问题 | `mvn spring-boot:run -Dspring-boot.run.profiles=local` 与 `data-service-app` 定向 Spring 集成测试均受 `queryController` Bean 冲突影响；按 `-pl data-service-app` 跑测试时还会暴露 `CredentialCodec` 依赖装配缺口 | 本地开发启动、后端集成验收复跑、CI 基线 | 未解决 | 后端 |
| R-02 | 测试问题 | `pnpm test:e2e:local` 默认 `4 workers` 并发会出现 `ECONNREFUSED 127.0.0.1:8081`，无法作为正式验收口径 | 全量 E2E 回归、CI 并发执行稳定性 | 已识别，已通过串行命令临时规避 | 前端 / 后端 / 测试 |
| R-03 | 覆盖问题 | Oracle 当前只完成环境、连接与基础数据前置验证，尚未补齐浏览器级或 API 级深度执行自动化 | 异构联邦专项回归、长期验收覆盖率 | 已识别待补 | 后端 / 测试 |

## 3. 分类汇总

| 分类 | 数量 | 说明 |
| --- | --- | --- |
| 环境问题 | 1 | 直接影响本地启动与 Spring 集成测试复跑 |
| 测试问题 | 1 | 影响默认 E2E 命令作为正式口径的可重复性 |
| 覆盖问题 | 1 | 影响 Oracle 相关深度自动化覆盖完整度 |

## 4. 判定说明

| 项目 | 内容 |
| --- | --- |
| 是否阻断本轮验收 | 否 |
| 原因 | 功能主链路、串行 E2E、运行态审计与核心资源保护单元基线均已通过；遗留问题均有明确影响边界和后续整改路径 |
