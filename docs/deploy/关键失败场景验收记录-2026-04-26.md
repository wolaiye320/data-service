# 关键失败场景验收记录-2026-04-26

## 1. 范围与目标

| 项目 | 内容 |
| --- | --- |
| 对应 WBS | `14.4.2` 复核关键失败场景验收结果 |
| 目标 | 复核权限失败、资源保护、联邦失败、停用场景等关键失败路径是否按预期受控失败 |
| 证据来源 | `pnpm exec playwright test --config=playwright.config.ts --reporter=list --workers=1` 串行验收结果 |

## 2. 关键失败场景结果

| 失败场景 | 自动化证据 | 结果 | 说明 |
| --- | --- | --- | --- |
| 管理接口缺少管理员请求头 | `e2e/auth-audit.spec.ts` 第 1 条 | 通过 | 页面加载失败且接口返回 `ACCESS_DENIED` |
| 查询缺少 `callerId` | `e2e/auth-audit.spec.ts` 第 1 条 | 通过 | 统一查询接口返回稳定未授权错误 |
| 查询租户越权 | `e2e/auth-audit.spec.ts` 第 2 条 | 通过 | 返回 `ACCESS_DENIED`，并生成 `QUERY_TENANT_ACCESS_DENIED` 审计 |
| 批量参数类型错误 | `e2e/query.spec.ts` 第 2 条 | 通过 | 返回 `PARTIAL_FAILURE`，错误项结构稳定 |
| 未发布服务被调用 | `e2e/query.spec.ts` 第 3 条 | 通过 | 返回 `RESOURCE_CONFLICT` |
| 批量超限 | `e2e/query.spec.ts` 第 5 条 | 通过 | 返回资源保护错误，未出现长时间挂起 |
| 联邦能力不足 | `e2e/federated-query-failure.spec.ts` 第 1 条 | 通过 | 发布阶段明确提示 `PROJECT_PUSHDOWN` 等能力缺口 |
| 联邦不兼容表达式 | `e2e/federated-query-failure.spec.ts` 第 2 条 | 通过 | 保存阶段明确提示复杂表达式不受支持 |
| 预览参数错误 | `e2e/services.spec.ts` 第 5 条前半段 | 通过 | 预览执行失败并回显参数类型不匹配 |
| 预览权限拒绝 | `e2e/services.spec.ts` 第 5 条后半段 | 通过 | 返回 `ACCESS_DENIED`，页面可见 `traceId` |
| 服务停用后不可继续调用 | `e2e/services.spec.ts` 第 3 条 + 功能验收记录映射 | 通过 | 停用动作成功，后续纳入正式调用限制验收口径 |

## 3. 复核结论

| 项目 | 结论 |
| --- | --- |
| 权限失败路径 | 通过 |
| 资源保护失败路径 | 通过 |
| 联邦失败路径 | 通过 |
| 预览失败路径 | 通过 |
| 关键失败场景整体结论 | 通过 |

## 4. 风险与说明

| 项目 | 内容 |
| --- | --- |
| 串行口径 | 失败场景复核基于串行验收命令；并发 4 worker 会引入环境级假失败，不纳入结论 |
| 停用后调用 | 本次浏览器验收已覆盖停用动作成功，若需单独留存“停用后统一查询接口调用失败”证据，可在后续专项回归中补一条 API 录屏或接口记录 |
