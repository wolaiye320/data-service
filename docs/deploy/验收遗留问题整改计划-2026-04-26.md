# 验收遗留问题整改计划-2026-04-26

## 1. 范围

| 项目 | 内容 |
| --- | --- |
| 对应 WBS | `14.6.2` 制定遗留问题整改计划 |
| 输入 | `验收遗留问题清单-2026-04-26.md` |
| 目标 | 为每个遗留问题明确整改动作、责任角色、完成日期和验收标准 |

## 2. 整改计划

| 编号 | 对应问题 | 整改目标 | 整改方案 | 责任角色 | 计划完成日期 | 验收标准 |
| --- | --- | --- | --- | --- | --- | --- |
| P-01 | R-01 | 恢复本地 `spring-boot:run` 与 `data-service-app` Spring 集成测试可重复执行 | 排查 `queryController` Bean 名冲突来源；统一组件扫描边界；修复 app-only 测试 classpath 下 `CredentialCodec` 依赖装配 | 后端 | `2026-04-28` | `mvn spring-boot:run -Dspring-boot.run.profiles=local` 可稳定启动；`mvn -q -pl data-service-app -am test -Dtest=QueryResourceProtectionIntegrationTest,QueryControllerIntegrationTest,AdminAuditLogControllerIntegrationTest,AdminConnectionControllerIntegrationTest` 可通过 |
| P-02 | R-02 | 让全量 E2E 在默认命令下具备稳定正式口径 | 收敛 Playwright 并发度、增强后端保活方式或隔离测试数据，消除 `4 workers` 下的 `ECONNREFUSED` | 前端 / 后端 / 测试 | `2026-04-29` | `pnpm test:e2e:local` 默认命令连续 2 次通过，且不再依赖手工串行兜底 |
| P-03 | R-03 | 补齐 Oracle 深度执行自动化回归 | 增加 Oracle 参与的单源或异构联邦自动化用例，并纳入专项回归清单 | 后端 / 测试 | `2026-04-30` | 至少 1 条 Oracle 查询执行自动化用例进入常规回归或专项回归文档，并产出可复用执行记录 |

## 3. 跟踪要求

| 项目 | 内容 |
| --- | --- |
| 跟踪频率 | 每日收口一次进展，直到 3 项问题全部关闭 |
| 状态约束 | 未达成验收标准前不得仅以“已知问题”口头关闭 |
| 文档更新 | 问题关闭后同步更新遗留问题清单、整改计划和后续验收报告附录 |
