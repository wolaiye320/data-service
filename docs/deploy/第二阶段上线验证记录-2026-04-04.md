# 第二阶段上线验证记录-2026-04-04

## 1. 范围与目标

| 项目 | 内容 |
| --- | --- |
| 对应 WBS | `5.2.2` 完成第二阶段上线验证 |
| 目标 | 验证第二阶段联邦 SQL 平台发布产物在当前运行环境中可正常启动、联邦管理端可访问、联邦统一查询摘要口径可用 |
| 前置依赖 | `5.2.1` 发布准备已完成 |

## 2. 验证环境

| 项目 | 内容 |
| --- | --- |
| 执行日期 | 2026-04-04 |
| 前端地址 | `http://127.0.0.1:3001` |
| 后端地址 | `http://127.0.0.1:8081` |
| 后端端口 | `8081` |
| 前端端口 | `3001` |
| 元数据库 | 本地 PostgreSQL `data_service` |
| 浏览器 | 本机 Google Chrome Headless |

## 3. 启动状态

| 项目 | 结果 | 说明 |
| --- | --- | --- |
| 前端进程 | 正常 | `3001` 端口存在 Vite 进程监听 |
| 后端进程 | 正常 | `8081` 端口存在 Spring Boot 进程监听 |
| 后端健康检查 | 通过 | `GET /actuator/health` 返回 `UP` |

## 4. 接口与链路验证

| 验证项 | 命令 / 入口 | 结果 | 说明 |
| --- | --- | --- | --- |
| 健康检查 | `GET /actuator/health` | 通过 | 返回 `{"status":"UP"}` |
| 平台基线接口 | `GET /api/platform/baseline` | 通过 | 返回资源保护与缓存指标信息 |
| 管理连接列表 | `GET /api/admin/connections` | 通过 | 携带 `X-Operator=web-admin`、`X-Operator-Role=ADMIN` 后返回启用连接 |
| 管理服务列表 | `GET /api/admin/service-definitions` | 通过 | 携带管理请求头后返回服务列表 |
| 联邦平台浏览器联调 | `cd data-service-web && pnpm test:e2e:local -- e2e/federation.spec.ts` | 通过 | 创建联邦草稿、保存联邦 SQL、校验诊断、发布动作主流程通过 |

## 5. 关键结果

| 项目 | 结论 |
| --- | --- |
| 端口验证 | 前端 `3001`、后端 `8081` 已按第二阶段发布准备要求生效 |
| 前后端代理 | 前端 `/api` 代理链路可用 |
| 联邦平台页面 | `/federation` 页面可完成草稿创建、SQL 保存、校验日志查看、计划诊断查看与发布动作 |
| 联邦管理接口 | 联邦相关管理接口在管理请求头下可正常返回 |
| 联邦发布闭环 | 浏览器长链路已验证“新建草稿 -> 保存并校验 -> 查看诊断 -> 发布”闭环 |

## 6. 风险与说明

| 项目 | 内容 |
| --- | --- |
| 当前记录边界 | 本记录覆盖当前环境下的启动与冒烟验证，不替代正式生产切流记录 |
| 数据样本依赖 | 联邦平台 E2E 会现场创建联邦服务草稿，环境清理后需重新准备联调数据 |
| Oracle 边界 | Oracle 容器验证不纳入本次常规上线验证命令 |
| 请求头约束 | 管理接口冒烟需显式携带 `X-Operator` 与 `X-Operator-Role=ADMIN`，否则会返回 `ACCESS_DENIED` |

## 7. 当前结论

| 项目 | 结论 |
| --- | --- |
| `5.2.2` | 已完成 |
| 下一步 | 可继续推进 `5.2.3` 培训、运维交接与最终验收材料整理 |
