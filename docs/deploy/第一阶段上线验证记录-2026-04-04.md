# 第一阶段上线验证记录-2026-04-04

## 1. 范围与目标

| 项目 | 内容 |
| --- | --- |
| 对应 WBS | `5.1.2` 完成第一阶段上线验证 |
| 目标 | 验证第一阶段发布产物在当前运行环境中可正常启动、对外提供服务并完成前后端主链路调用 |
| 前置依赖 | `5.1.1` 发布准备已完成 |

## 2. 验证环境

| 项目 | 内容 |
| --- | --- |
| 执行日期 | 2026-04-04 |
| 前端地址 | `http://127.0.0.1:3001` |
| 后端地址 | `http://127.0.0.1:8081` |
| 后端端口 | `8081` |
| 前端端口 | `3001` |
| 元数据库 | 本地 PostgreSQL `data_service` |
| 冒烟服务 | `query_debug_demo_v2` |

## 3. 启动状态

| 项目 | 结果 | 说明 |
| --- | --- | --- |
| 前端进程 | 正常 | `3001` 端口有 Vite 进程监听 |
| 后端进程 | 正常 | `8081` 端口有 Spring Boot 进程监听 |
| 后端健康检查 | 通过 | `GET /actuator/health` 返回 `UP` |

## 4. 接口与链路验证

| 验证项 | 命令 / 入口 | 结果 | 说明 |
| --- | --- | --- | --- |
| 健康检查 | `GET /actuator/health` | 通过 | 返回 `{"status":"UP"}` |
| 平台基线接口 | `GET /api/platform/baseline` | 通过 | 返回资源保护与缓存指标信息 |
| 管理连接列表 | `GET /api/admin/connections` | 通过 | 管理接口可返回已启用连接 |
| 管理服务列表 | `GET /api/admin/service-definitions` | 通过 | 返回 `customer_order_query`、`query_debug_demo_v2` |
| 统一查询冒烟 | `POST /api/data-services/query` | 通过 | `query_debug_demo_v2` 返回 `demoValue=Y` |
| 浏览器级联调 | `cd data-service-web && pnpm test:e2e:local` | 通过 | 2 个 E2E 用例全部通过 |

## 5. 关键结果

| 项目 | 结论 |
| --- | --- |
| 端口验证 | 前端 `3001`、后端 `8081` 已按发布准备要求生效 |
| 前后端代理 | 前端 `/api` 代理链路可用 |
| 管理端页面 | `/datasource`、`/catalog`、`/service`、`/publish`、`/query-debug` 主流程可用 |
| 查询调试闭环 | 输入 `query_debug_demo_v2 + demoFlag=Y` 可返回 `demoValue=Y` |
| 冒烟基线 | 发布后最小可用服务 `query_debug_demo_v2` 已可稳定用于上线验证 |

## 6. 风险与说明

| 项目 | 内容 |
| --- | --- |
| 当前记录边界 | 本记录覆盖当前环境下的启动与冒烟验证，不替代正式生产切流记录 |
| 环境依赖 | 前端 E2E 依赖本地已发布服务 `query_debug_demo_v2`，后续环境清理后需重新导入 |
| 启动说明 | 后端本地调试命令已统一为 `mvn -f data-service-app/pom.xml spring-boot:run -Dspring-boot.run.profiles=local` |

## 7. 当前结论

| 项目 | 结论 |
| --- | --- |
| `5.1.2` | 已完成 |
| 下一步 | 可继续推进 `5.1.3` 第一阶段验收材料整理 |
