# 数据服务-后端集成测试报告-2026-04-04

## 1. 执行范围

| 项目 | 内容 |
| --- | --- |
| 覆盖 WBS | `4.1.1` 第一阶段单元测试；`4.1.2` 第一阶段集成测试；`4.2.1` 第二阶段单元测试；`4.2.2` 第二阶段联邦场景集成测试 |
| 覆盖模块 | `data-service-application`、`data-service-infrastructure`、`data-service-app` |
| 覆盖能力 | 连接管理、服务配置与发布、统一查询、缓存、权限审计、平台基线、JDBC 连接适配、联邦预定义关联接口联调、联邦执行摘要接口验收 |

## 2. 执行环境

| 项目 | 内容 |
| --- | --- |
| 执行日期 | 2026-04-04 |
| 最近复执行时间 | 2026-04-04 21:29:11 ~ 2026-04-05 11:27:53 |
| Java | JDK 21 |
| 构建工具 | Maven |
| 元数据库 | 本地 PostgreSQL `data_service` |
| 说明 | Oracle 容器验证不纳入常规回归命令；容器型数据源测试已改为无 Docker 自动跳过；本次额外补测 MySQL / Oracle 真实连接、缓存键隔离、发布快照隔离、来源一致性校验与权限审计缺口 |

## 3. 本次新增测试

| 类型 | 文件 | 覆盖点 |
| --- | --- | --- |
| 单元测试 | `data-service-application/src/test/java/cn/dtkeys/dataservice/application/service/ConnectionManagementServiceTest.java` | 已发布服务引用连接禁止停用、状态更新审计、连接脱敏展示 |
| 单元测试 | `data-service-application/src/test/java/cn/dtkeys/dataservice/application/service/ServiceDefinitionManagementServiceTest.java` | 发布前字段校验、发布版本快照、草稿服务禁止停用 |
| 单元测试 | `data-service-application/src/test/java/cn/dtkeys/dataservice/application/service/FederatedMetadataManagementServiceTest.java` | 联邦 SQL 草稿保存、非联邦服务拦截、方言不支持拦截、来源能力替换 |
| 单元测试 | `data-service-federation/src/test/java/cn/dtkeys/dataservice/federation/FederatedPipelineTest.java` | Parser、Validator、Planner、Optimizer、诊断、启发式退化、落盘缓冲、分波并发 |
| 单元测试 | `data-service-federation/src/test/java/cn/dtkeys/dataservice/federation/executor/PredefinedJoinQueryExecutorTest.java` | 同源下推、异源 LOOKUP 回退、执行摘要输出、关联键约束 |
| 接口集成测试 | `data-service-app/src/test/java/cn/dtkeys/dataservice/app/QueryControllerIntegrationTest.java` | 联邦 LOOKUP/PUSHDOWN 分流、联邦超时阈值、落盘缓冲摘要返回、执行摘要字段返回 |
| 接口集成测试 | `data-service-app/src/test/java/cn/dtkeys/dataservice/app/AdminManagementIntegrationTest.java` | 单连接多目标库、发布审计、停用审计、已发布版本与草稿隔离 |
| 接口集成测试 | `data-service-app/src/test/java/cn/dtkeys/dataservice/app/AuditAndPermissionIntegrationTest.java` | 非授权角色测试连接拒绝 |
| 组件集成测试 | `data-service-app/src/test/java/cn/dtkeys/dataservice/app/QueryExecutionComponentsIntegrationTest.java` | 发布态运行定义加载发布快照，不受草稿 SQL 变更污染 |
| 容器集成测试 | `data-service-infrastructure/src/test/java/cn/dtkeys/dataservice/infrastructure/datasource/DatasourceConnectionManagerContainerIntegrationTest.java` | MySQL / Oracle 真实容器连接验证 |

## 4. 执行命令

| 项目 | 命令 | 结果 |
| --- | --- | --- |
| 应用层单元测试 | `mvn -pl data-service-application -am test -Dtest=ConnectionManagementServiceTest,ServiceDefinitionManagementServiceTest -Dsurefire.failIfNoSpecifiedTests=false` | 通过 |
| 联邦单元测试 | `mvn -pl data-service-application,data-service-federation -am test -Dtest=FederatedMetadataManagementServiceTest,FederatedPipelineTest,PredefinedJoinQueryExecutorTest -Dsurefire.failIfNoSpecifiedTests=false` | 通过 |
| 基础设施层测试 | `mvn -pl data-service-infrastructure -am test -Dtest=MetadataRepositoryIntegrationTest,DatasourceConnectionManagerTest -Dsurefire.failIfNoSpecifiedTests=false` | 通过 |
| 应用层集成测试 | `mvn -pl data-service-app -am test -Dtest=AdminManagementIntegrationTest,QueryControllerIntegrationTest,QueryExecutionComponentsIntegrationTest,AuditAndPermissionIntegrationTest,PlatformBaseIntegrationTest -Dsurefire.failIfNoSpecifiedTests=false` | 通过 |
| 第二轮补测 | `mvn -pl data-service-application,data-service-app -am test -Dtest=FederatedMetadataManagementServiceTest,QueryExecutionComponentsIntegrationTest,AuditAndPermissionIntegrationTest,QueryControllerIntegrationTest,AdminManagementIntegrationTest -Dsurefire.failIfNoSpecifiedTests=false` | 通过 |
| 容器适配补测 | `mvn -pl data-service-infrastructure -am test -Dtest=DatasourceConnectionManagerContainerIntegrationTest -Dsurefire.failIfNoSpecifiedTests=false` | 通过 |
| 预览执行审计补测 | `mvn -pl data-service-app -am test -Dtest=AdminManagementIntegrationTest -Dsurefire.failIfNoSpecifiedTests=false` | 通过 |
| 联邦解析回归 | `mvn -pl data-service-federation test -Dtest=FederatedPipelineTest -Dsurefire.failIfNoSpecifiedTests=false` | 通过 |
| 真实联邦 SQL 补测 | `TESTCONTAINERS_RYUK_DISABLED=true mvn -pl data-service-app -am test -Dtest=QueryControllerIntegrationTest#shouldExecuteFederatedSqlAcrossSamePostgresqlTypeSources+shouldExecuteFederatedSqlAcrossPostgresqlAndMysqlSources -Dsurefire.failIfNoSpecifiedTests=false` | 通过 |
| 联邦相关扩大回归 | `TESTCONTAINERS_RYUK_DISABLED=true mvn -pl data-service-application,data-service-federation,data-service-app -am test -Dtest=FederatedMetadataManagementServiceTest,FederatedPipelineTest,AdminManagementIntegrationTest,QueryControllerIntegrationTest -Dsurefire.failIfNoSpecifiedTests=false` | 通过 |

## 4.1 本次复执行结果摘要

| 项目 | 结果 |
| --- | --- |
| 执行批次 | 按《数据服务-集成测试用例设计》已自动化主链路复执行 |
| 总测试数 | 61 |
| 失败数 | 0 |
| 错误数 | 0 |
| 结论 | 文档中已自动化的 PostgreSQL 主回归链路本次复执行全部通过 |

## 4.2 本次补测结果摘要

| 项目 | 结果 |
| --- | --- |
| 补测范围 | `IT-CONN-002`、`IT-CONN-003`、`IT-CONN-005`、`IT-QUERY-003`、`IT-QUERY-008`、`IT-QUERY-009`、`IT-FED-007`、`IT-FED-008`、`IT-FED-009`、`IT-GOV-002`、`IT-GOV-003`、`IT-GOV-004`、`IT-GOV-005`、`IT-GOV-008` |
| 应用层补测结果 | `50` 测试通过，`0` 失败，`0` 错误 |
| 容器补测结果 | `2` 测试通过，`0` 失败，`0` 错误 |
| 未完成项 | `无` |
| 结论 | 《数据服务-集成测试用例设计》中的正式未执行集成用例已全部补齐并拿到真实执行证据 |

## 5. 结果汇总

| 分类 | 测试类 | 结果 | 说明 |
| --- | --- | --- | --- |
| 单元测试 | `ConnectionManagementServiceTest` | 通过 | 新增 3 条应用层规则测试 |
| 单元测试 | `ServiceDefinitionManagementServiceTest` | 通过 | 新增 3 条服务发布规则测试 |
| 单元测试 | `FederatedMetadataManagementServiceTest` | 通过 | 联邦元数据保存、能力替换、规划失败拦截通过 |
| 单元测试 | `FederatedPipelineTest` | 通过 | 联邦主链路、启发式退化、落盘缓冲、依赖图分波并发通过 |
| 单元测试 | `PredefinedJoinQueryExecutorTest` | 通过 | PUSHDOWN / LOOKUP 双路径与执行摘要输出通过 |
| 仓储/连接测试 | `DatasourceConnectionManagerTest` | 通过 | 覆盖 PostgreSQL / MySQL / Oracle JDBC URL 生成 |
| 仓储集成测试 | `MetadataRepositoryIntegrationTest` | 通过 | Flyway、Repository CRUD、审计仓储查询通过 |
| 接口集成测试 | `AdminManagementIntegrationTest` | 通过 | 连接管理、服务草稿、发布停用链路、联邦 SQL 预览执行审计通过 |
| 接口集成测试 | `QueryControllerIntegrationTest` | 通过 | 统一查询、批量查询、缓存、资源限制、跨源组合查询、真实同构/异构联邦 SQL 执行与联邦执行摘要返回通过 |
| 组件集成测试 | `QueryExecutionComponentsIntegrationTest` | 通过 | 运行时定义加载、只读校验、参数绑定、结果映射通过 |
| 接口集成测试 | `AuditAndPermissionIntegrationTest` | 通过 | 审计查询与权限拦截通过 |
| 接口集成测试 | `PlatformBaseIntegrationTest` | 通过 | 平台基线接口与审计记录通过 |
| 容器集成测试 | `DatasourceConnectionManagerContainerIntegrationTest` | 通过 | MySQL / Oracle 真实 JDBC 连接验证通过 |

## 6. 关键结论

| 项目 | 结论 |
| --- | --- |
| `4.1.1` | 第一阶段应用层关键业务规则已补齐单元测试并通过 |
| `4.1.2` | 第一阶段后端核心链路已有可重复执行的真实 PostgreSQL 集成测试并通过 |
| `4.2.1` | 第二阶段联邦元数据、联邦执行链路、预定义关联执行器的单元测试已补齐并通过 |
| `4.2.2` | 第二阶段联邦接口链路已补齐 LOOKUP/PUSHDOWN、联邦超时、大结果集缓冲摘要、执行摘要字段集成测试并通过 |
| 本轮补测 | 已补齐真实 MySQL / Oracle 连接验证、单连接多目标库、配置错误返回配置错误、缓存键隔离、来源一致性校验、非授权测试连接、发布停用审计、预览执行审计、发布快照隔离、真实同构/异构联邦 SQL 最终执行器验证 |
| 回归约束 | 容器依赖测试不再阻塞常规回归；Oracle 验证继续保持独立执行 |
| 风险 | `data-service-app` 与 `data-service-infrastructure` 的集成测试均依赖共享本地 PostgreSQL，后续执行应严格串行运行，避免并发 `truncate` 干扰；异构联邦 SQL 集成测试依赖本地 Docker/Testcontainers，可继续保持 `TESTCONTAINERS_RYUK_DISABLED=true` 执行口径；查询端整组回归曾暴露 `DatasourceConnectionManager` 进程级缓存复用导致的跨用例污染，现已在测试中显式清缓存并复验通过 |
