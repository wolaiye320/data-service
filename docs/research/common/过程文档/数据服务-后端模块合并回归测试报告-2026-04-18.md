# 数据服务-后端模块合并回归测试报告-2026-04-18

## 1. 执行范围

| 项目 | 内容 |
| --- | --- |
| 重构目标 | 将 `data-service-interfaces`、`data-service-application`、`data-service-domain`、`data-service-infrastructure`、`data-service-federation` 合并为 `data-service-core` |
| 覆盖模块 | `data-service-common`、`data-service-core`、`data-service-app` |
| 覆盖内容 | Maven 聚合、Spring 扫描、MyBatis 扫描、Flyway 路径、核心测试入口、联邦定向测试脚本 |

## 2. 执行环境

| 项目 | 内容 |
| --- | --- |
| 执行日期 | 2026-04-18 |
| 最近执行时间 | 2026-04-18 20:21:34 CST |
| Java | JDK 21 |
| Maven | Maven 3.9+ |
| 元数据库 | 本地 PostgreSQL `data_service` |
| 说明 | 本次完成模块合并、编译闭环、核心真实数据库回归、启动模块 HTTP 集成回归与联邦脚本入口回归；旧历史测试报告仅作为合并前基线 |

## 3. 本次代码与文档变更

| 类型 | 路径 | 说明 |
| --- | --- | --- |
| 新增模块 | `data-service-core/` | 承载原 5 个业务模块主代码、测试、Flyway 迁移 |
| 构建调整 | `pom.xml` | 后端聚合模块改为 `data-service-common`、`data-service-core`、`data-service-app` |
| 启动调整 | `data-service-app/pom.xml` | 启动模块只依赖 `data-service-core` 与 `data-service-common` |
| 扫描调整 | `data-service-app/src/main/java/cn/dtkeys/dataservice/app/config/MybatisRepositoryConfiguration.java` | Mapper 扫描切到 `cn.dtkeys.dataservice.repository` |
| Hook 调整 | `bin/git/git-hook.project.config.sh` | 后端模块与根测试命令切到新结构 |
| 联邦脚本调整 | `bin/test-federation.sh`、`bin/README.md` | 联邦定向测试模块改为 `data-service-core` |
| 主文档调整 | `docs/research/common/工程架构说明.md` | 工程骨架整体切换到 `app + common + core` |

## 4. 执行命令

| 项目 | 命令 | 结果 |
| --- | --- | --- |
| 第一次编译探测 | `mvn -q -pl data-service-core,data-service-app -am test -DskipTests` | 通过 |
| 删除旧模块后二次编译 | `mvn -q -pl data-service-core,data-service-app -am test -DskipTests` | 通过 |
| package 重写后复编译 | `mvn -q -pl data-service-core,data-service-app -am test -DskipTests` | 通过 |
| 核心模块真实回归 | `mvn -q -pl data-service-core -am test -Dtest=CacheKeyGeneratorTest,CachePolicyResolverTest,InMemoryQueryCacheTest,DatasourceConnectionManagerTest,QueryParameterBinderTest,QueryResultMapperTest,SqlReadOnlyValidatorTest,ResourceProtectionServiceTest,MetadataRepositoryIntegrationTest,FederatedPipelineTest,PredefinedJoinQueryExecutorTest,ConnectionManagementServiceTest,FederatedMetadataManagementServiceTest,ServiceDefinitionManagementServiceTest -Dsurefire.failIfNoSpecifiedTests=false` | 通过 |
| 启动模块 HTTP 集成回归 | `mvn -q -pl data-service-app -am test -Dtest=AdminManagementIntegrationTest,AuditAndPermissionIntegrationTest,PlatformBaseIntegrationTest,QueryControllerIntegrationTest,QueryExecutionComponentsIntegrationTest -Dsurefire.failIfNoSpecifiedTests=false` | 通过 |
| 联邦脚本入口回归 | `./bin/test-federation.sh` | 通过，13 个测试通过，0 失败，0 跳过 |

## 5. 覆盖结论

| 项目 | 结果 |
| --- | --- |
| Maven 聚合关系 | 已切换到 `data-service-common -> data-service-core -> data-service-app` |
| Spring 组件扫描 | 通过，`data-service-app` 可聚合 `data-service-core` |
| MyBatis Mapper 扫描 | 通过，扫描路径已切到 `cn.dtkeys.dataservice.repository` |
| Flyway 迁移路径 | 已迁入 `data-service-core/src/main/resources/db/migration/` |
| Flyway 迁移校验 | 已在本地 PostgreSQL `data_service` 验证 5 个 migration，当前 schema 为版本 5 |
| 核心源码结构 | 已按 `datasource/service/query/federation/audit/security/repository/web/config` 收敛 |
| 旧模块目录 | 已删除 |

## 6. 已覆盖接口与场景

| 类别 | 已覆盖项 |
| --- | --- |
| 构建入口 | 根聚合 POM、`data-service-app` 依赖、新 `data-service-core` 依赖收敛 |
| 运行装配 | 配置属性类、权限拦截器、Mapper 扫描、Boot 启动聚合 |
| 核心业务包 | 数据源、服务定义、查询执行、联邦能力、审计、安全、Repository |
| Repository 与 Schema | `MetadataRepositoryIntegrationTest` 使用本地 PostgreSQL 验证 Repository 映射与 Flyway schema |
| 数据源与执行组件 | `DatasourceConnectionManagerTest`、参数绑定、只读校验、结果映射、缓存策略、资源保护 |
| 业务编排 | 连接管理、服务定义管理、联邦元数据管理 |
| HTTP 与权限链路 | 平台基线、查询接口、管理接口、审计查询、权限拦截 |
| 联邦链路 | Parser、Validator、Planner、Optimizer、Executor、预定义 Join、联邦预览与发布链路 |
| 测试入口 | `data-service-core` 单模块测试、`data-service-app` 启动模块测试、联邦定向脚本、Git hook 根测试命令 |

## 7. 未覆盖项

| 项目 | 说明 |
| --- | --- |
| 全量 `mvn test` | 本次未执行全仓无过滤测试，已执行受重构影响的核心模块与启动模块定向回归 |
| Oracle 容器连接专项 | 本次未单独重跑 `DatasourceConnectionManagerContainerIntegrationTest` 的 Oracle 容器路径；旧报告保留合并前基线 |
| 性能专项报告 | 本次已通过 `ResourceProtectionServiceTest` 与联邦缓冲相关测试，尚未新增独立性能专项记录 |

## 8. 风险项

| 风险 | 说明 |
| --- | --- |
| 历史过程文档仍引用旧模块名 | 旧报告保留历史事实，不代表当前结构；后续新增报告必须统一使用 `data-service-core` |
| 全量无过滤测试未执行 | 本次已覆盖受模块合并影响的关键链路，但仍建议在提交前或 CI 中补跑一次全量 `mvn test` |
| 本地共享 PostgreSQL 干扰 | 后续跑集成测试时仍需串行执行，避免共享库 `truncate` 互相污染 |
| Docker/Testcontainers 依赖 | MySQL 容器链路已随启动模块回归通过；Oracle 容器专项仍建议在有足够本地资源时单独复验 |

## 9. 结论

| 项目 | 结论 |
| --- | --- |
| 模块合并 | 已完成 |
| 包结构收敛 | 已完成 |
| 旧模块删除 | 已完成 |
| 编译闭环 | 已完成 |
| 真实数据库回归 | 已完成 |
| 启动模块 HTTP 集成回归 | 已完成 |
| 联邦脚本入口回归 | 已完成 |
| 后续动作 | 提交前建议补跑全量 `mvn test`，并按需补 Oracle 容器专项与性能专项记录 |
