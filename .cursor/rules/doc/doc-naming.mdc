---
description: 文档命名原子规则（全中文、减号分隔、无空格括号）
globs: "docs/**/*.md"
alwaysApply: false
---

# 文档命名原子规则

## 1) 目标
在 `docs/` 下形成**可读、可检索、可维护**的文件名；避免随着规模增长出现“看不懂、搜不到、改不动”。

## 2) 强制要求
- **全中文**：同一文件名中禁止中英混写（技术专有名词除外，但优先用中文表达）。
- **减号分隔**：仅使用英文减号 `-` 分隔语义段。
- **禁止字符**：空格、下划线 `_`、括号 `()` `（）`、全角破折号 `—`、斜杠 `/`、反斜杠 `\`。
- **扩展名**：统一 `.md`。

## 3) 命名模板（直接复用）
- **推荐**：`<一级功能>-<二级功能>-<描述>.md`
- **允许追加限定词**（同名区分）：`<一级功能>-<二级功能>-<描述>-<限定词>.md`

示例：
- `数据管理-质量监测站-验收脚本-银行业.md`
- `系统管理-租户管理-需求规格说明书.md`

## 4) 与目录结构的配合
- **目录承载分级**：优先把“一级/二级功能”放进目录（例如 `docs/research/data-mgmt/`），文件名只保留“页面短名”与必要限定词。
- 若目录层级不足以表达语义，再使用上面的模板把分级补进文件名，但仍保持全中文与减号分隔。

## 5) 最低校验（可用于 githooks/CI）
只要出现空格/下划线/括号/全角符号，即视为命名违规：

```bash
find docs -type f -name '*.md' | grep -E '[ _（）()—]' \
  && echo "存在命名违规（空格/下划线/括号/全角破折号）" \
  || echo "命名检查通过"
```

