---
description: 文档维护原子规则（链接、资源、废弃迁移、索引更新）
globs: "docs/**/*.md"
alwaysApply: false
---

# 文档维护原子规则

## 1) 链接规范（强制）
- 同仓库文档一律使用**相对路径**，禁止粘贴本机绝对路径。
- 引用别的文档时，优先链接到**稳定入口**（例如各目录 `README.md` 或“总览页”）。

## 2) 图片/附件（强制）
- 图片与附件统一放到 `docs/assets/`（可按主题再分子目录，如 `docs/assets/截图/`）。
- 资源文件名同样遵循“全中文 + 减号分隔 + 无空格括号”。

## 3) 重命名与迁移（强制）
- 文档一旦被外部引用或跨模块引用，原则上不直接改名。
- 必须迁移时，采用：
  - **新文档**：在目标位置创建并补全内容
  - **旧文档保留**：在开头增加“已迁移/替代文档路径/生效版本/废弃日期”说明，并给出新文档相对链接
  - **索引同步**：更新所在目录的 `README.md`（或上级索引）指向新文档

## 4) 目录索引维护（推荐）
- 每个分类目录（如 `docs/research/`、`docs/guide/`）建议维护一个 `README.md`，提供：
  - 目录说明（存什么、不存什么）
  - 重点文档清单（表格 + 相对链接）
  - 状态字段（草案/已评审/执行中/已验收）

## 5) 补充优先于新建（强制）
- **需求类**：新需求、子需求、细化条款 → 在对应模块现有「需求规格说明书」中补充章节。
- **设计类**：新方案、实施方案、技术落地点 → 在对应模块现有「架构与详细设计」中补充章节。
- **例外**：全新业务模块（独立 WBS 一级）、跨模块通用规范可新建文档，但需在模块文档中引用。

## 6) 最低校验（可选但推荐）
- **断链检查**：对新增文档至少自检一次相对链接可达。
- **命名检查**：复用 `doc-naming.mdc` 的最低校验脚本。

