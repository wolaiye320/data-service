---
description: 文档管理规范入口（索引与快速规则）
globs: "docs/**/*.md"
alwaysApply: false
---

# 文档管理规范（入口索引）

这份文件只保留“**快速规则** + **索引**”，避免把所有规范堆在一个 `alwaysApply: true` 的大文件里导致常驻开销与维护困难。

## 快速规则（常用 5 条）
- **目录承载分级**：一级/二级功能尽量放到目录中表达；文件名保持“页面短名”。
- **全中文 + 减号分隔**：文件名默认全中文，使用 `-` 分隔，禁止空格/括号/下划线。
- **分类优先**：优先落在 `docs/guide/`、`docs/research/`、`docs/api/`、`docs/design/`、`docs/deploy/`、`docs/assets/` 等分类目录下。
- **根目录只放总览**：`docs/` 根目录仅用于工程架构/技术选型/数据库规约/UI 规范等项目级总览类文档。
- **相对链接**：同仓库文档一律用相对路径引用。
- **迁移不删**：重命名/迁移采用“新文档 + 旧文档标注替代链接 + 更新索引”，避免直接删除造成断链。

- **补充优先于新建**：新需求规格、设计方案、实施方案等，在现有需求规格书、架构与详细设计文档中进行细化补充，不新建零碎文档。

## 原子规则（建议按需引用）
- 命名：`.cursor/rules/doc/doc-naming.mdc`
- 写作：`.cursor/rules/doc/doc-writing.mdc`
- 维护：`.cursor/rules/doc/doc-maintenance.mdc`



