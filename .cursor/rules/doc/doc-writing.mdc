---
description: 文档写作原子规则（结构、标题、示例、可读性）
globs: "docs/**/*.md"
alwaysApply: false
---

# 文档写作原子规则

## 1) 页面结构（建议模板）
按需裁剪，但建议保持“先结论、后细节”：
- **目的**：这篇文档解决什么问题（读完能做什么）
- **范围**：适用模块/版本/不包含内容
- **背景**：现状与痛点（必要时）
- **方案/设计**：关键决策、取舍、接口/数据结构
- **使用/操作步骤**：面向执行者的步骤化说明
- **验证/验收**：如何自测、如何验收、关键口径
- **风险与回滚**：前置条件、风险点、回滚策略
- **变更记录**（可选）：重要变更的时间与原因

## 2) 标题与段落
- 标题层级建议不超过 4 层：`#` / `##` / `###` / `####`
- 段落短句化，避免超长段落；能列表就列表。
- 关键规则/口径用**加粗**标出，便于扫读。

## 3) 表格与清单
- 对比类信息优先用表格（字段、口径、状态、责任人、截止时间）。
- 操作步骤优先用有序列表；验收项用勾选清单。

## 4) 代码与命令示例
- 示例必须**可复制、可执行**（至少在同操作系统/同版本下成立）。
- 代码块需注明语言：`bash`、`sql`、`json`、`java`、`ts` 等。
- 示例写清楚“输入/输出/预期行为”，避免只有片段没有上下文。

## 5) 术语与命名
- 文内术语与系统实际对象（模块名、表名、接口）保持一致。
- 避免同一概念多种叫法；首次出现可给出中文解释与英文缩写。

