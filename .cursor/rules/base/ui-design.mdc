---
description: 前端 UI 设计规范，新页面开发必须遵循
globs: /**/*.{tsx,ts,css}
alwaysApply: false
---

# 数智灵犀 UI 设计规范

**完整规范**：见 `docs/UI设计规范.md`。开发新页面时必读并严格遵循。

## 布局

- **无顶栏**：品牌、用户、通知均在左侧栏，不单独使用 Header。
- **主内容区 Layout 全部不留间隙**：主内容区 `Layout` 使用 `padding: 0`，与侧栏、视口边缘之间不留空隙；`Content` 使用 `borderRadius: 0` 贴边。
- **主内容区**：页面内容放在 `Content` 内，使用 `className="li-content-card"`，卡片内边距 24px。
- **根布局**：`Layout` 使用 `flexDirection: 'row'`，侧栏 220px，内容区 `flex: 1`。

## 颜色与 Token

- **主色**：`#1677ff`；侧栏背景 `#1e3a5f`；选中项 `#3b82f6`。
- **间距**：优先使用 `--li-space-*`（xs/sm/md/lg/xl）。
- **卡片**：使用 `--li-card-shadow`、`--li-card-shadow-hover`。

## 页面结构

1. **页面容器**：使用 `className="li-page-wrapper"` 包裹页面根元素；主内容 Card 加 `li-page-main-card`，保证最小高度 360px，减少页面切换时的抖动。
2. **标题行**：使用 `PageHeader` 或 `Typography.Title level={5}`，操作按钮右对齐；`PageHeader` 固定 `minHeight: 32`，有无按钮均保持一致高度。
3. **表格**：`Table size="small"`，加 `className="li-table-zebra"`；操作列**仅图标 + Tooltip**，不配文字，`type="text"`；类型、状态等列用**普通文本**，不用彩色 Tag。
4. **空状态**：使用 `EmptyState` 组件，提供引导文案和操作。
5. **弹窗/抽屉/新页面选择**：字段约 ≤8 个 → 弹窗；需滚动 → 抽屉；复杂长表单（30 分钟以上）→ 新页面；删除一律用弹窗或 Popconfirm；新增与编辑保持一致；禁止弹窗套弹窗。详见 `docs/UI设计规范.md` §9。
6. **Modal**：使用 `destroyOnClose`；弹窗内表单须**一屏内完整展示**（含标题、表单、底部按钮），多字段时用两列布局（Row/Col）、控件 `size="small"`、表单容器加 `className="li-form-compact"`（见 `index.css`）。
7. **表单易用性**：**能使用下拉框就尽量使用下拉框**。有有限可选集合的栏位用 `Select`（固定枚举或接口拉取的列表），带 `showSearch` 便于检索；选项来自接口且可能为空时，可回退为 `Input` 并提示「暂无下拉数据，可手动输入」。依赖前序选择的栏位占位符要明确（如「请先选择数据源」）。详见 `docs/UI设计规范.md` §11。

## 组件与类名

| 用途       | 组件/类名                     |
|------------|-------------------------------|
| 页面标题   | `PageHeader`（含 `li-page-header`） |
| 空状态     | `EmptyState`                  |
| 页面容器   | `li-page-wrapper`             |
| 主内容卡片 | `li-page-main-card`           |
| 内容卡片   | `li-content-card`             |
| 表格斑马纹 | `li-table-zebra`              |
| 选中行高亮 | `li-table-row-selected`       |
| 弹窗紧凑表单 | `li-form-compact`（表单项间距缩小） |

## 侧栏导航

- **两级信息架构**：一级菜单表示业务域，二级菜单表示具体功能页面。
- **数据管理**：一级菜单「数据管理」，二级菜单包含「连接配置」「权限配置」「质量规则」。
- **新增功能**：优先挂在既有业务域下；仅当形成独立业务域时才新增一级菜单。
- **配置位置**：在 `AppSidebar.tsx` 的 `menuItems` 中配置，格式保持一致（icon + Link 或 children）。
