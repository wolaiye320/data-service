---
description: 使用 React Hook Form 进行表单处理的最佳实践
globs: *.tsx,*.ts
alwaysApply: false
---

- 使用 `useForm` Hook 高效管理表单状态
- 使用 Zod 搭配 `@hookform/resolvers` 实现类型安全（type-safe）的表单校验
- 使用 `Controller` 组件集成自定义输入
- 利用 `useFormContext` 在组件间共享表单状态