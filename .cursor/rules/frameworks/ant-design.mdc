---
description: 前端 UI 使用 Ant Design 的约定与最佳实践
globs: linkinsight-web/**/*.{tsx,ts,jsx,js}
alwaysApply: false
---

# Ant Design 前端 UI 规范

- **选型**：中后台界面统一使用 Ant Design（antd）作为 UI 组件库，图标使用 `@ant-design/icons`。
- **文档**：https://ant.design/components/overview-cn

## 使用约定

- **入口**：在应用根节点使用 `<ConfigProvider>` 包裹，统一语言、主题等；中文界面设置 `locale={zhCN}`。
- **布局**：列表/管理类页面使用 `<Layout>` + 侧边/顶栏导航；内容区使用 `<Card>`、`<Space>` 组织。
- **表格**：数据列表使用 `<Table>`，配置 `columns`、`dataSource`、`loading`、`rowKey`，支持分页 `pagination={{ pageSize: 10 }}`。
- **表单**：新建/编辑使用 `<Form>` + `<Modal>` 或 `<Drawer>`；校验使用 `rules`，提交用 `onFinish`。
- **操作**：按钮使用 `<Button type="primary">`、`<Button type="link">`；危险操作配合 `<Popconfirm>`；批量操作使用 `<Space>` 包裹。
- **反馈**：成功/失败提示用 `message.success()`、`message.error()`；确认用 `Modal.confirm()`；加载态用 `<Spin>` 或 `Table` 的 `loading`。
- **图标**：操作按钮优先使用 `@ant-design/icons` 中的图标（如 `PlusOutlined`、`EditOutlined`、`DeleteOutlined`）。

## 与 Tailwind 的关系

- Ant Design 负责组件与交互；Tailwind 用于布局微调、间距、与自定义样式，避免对 antd 组件做侵入式覆盖。
- 自定义类名可与 antd 的 `className` 并存，保持样式优先级可控。

## 原则

- 非必要不自行实现已由 antd 提供的组件（如 Table、Form、Modal）。
- 新页面优先采用 Ant Design 组件搭建，保证企业级一致性与可维护性。
