# AGENTS 指南

面向本仓库内 AI Agent 的生产级执行规范。目标是统一行为边界、降低歧义、提升实现质量，并确保代码、文档、测试与协作流程可持续维护。
---

## 1. 总体执行原则

### 1.1 响应语言与输出风格

- 始终使用中文回复用户
- 技术专有名词可保留英文，如 API、Java、DTO、Flyway、MyBatis、Playwright
- 输出尽可能简洁，只保留对当前任务直接有帮助的信息
- 禁止无效寒暄、重复铺垫、空泛总结
- 默认延续项目既有风格，不主动引入新的代码风格、文档风格或交互风格，包括代码、文档、命名、缩进、交互与结构组织
- 表格化输出:多数内容(尤其是评审、对比、多项任务)必须以 Markdown 表格输出。
- 强制收口:结束对话必须明确告知用到的skill。

### 1.2 基本行为准则 

- 先理解上下文，再执行修改，避免基于猜测直接改动
- 优先保持现有结构稳定，避免过度设计、过度抽象和无关重构
- 任何实现都必须兼顾可测试性、可维护性、可扩展性和可读性
- 非必要不引入新依赖、新框架、新组件体系，尤其避免“传染性”强的技术选型，需要引入时请先请示是否引入，引入时先更新技术选型文档
- 复杂功能若必须引入第三方能力，优先评估中国开发者社区成熟、稳定、可维护的方案
- 不得忽视异常路径、边界条件、输入校验、并发安全、性能影响和资源释放
- 功能性代码变更必须补齐对应测试；数据库变更必须补齐迁移脚本与真实数据库验证
- 第一性原理:从原始需求出发。动机不清立刻停，路径非最优直接纠正。
- 极简沟通:用简单直白的中文一次性输出，把用户当高中生。拒绝角色扮演，拒绝分段分口吻，对话中已解决的问题后续绝不再提。不要用PO/P1/P2这种术语。
- 发现问题尽早暴露。严禁使用任何降级、兜底、后发式补丁或非严谨通用算法的后处理补救。
- 自检与精简:每次改动后，严格执行Review查 Bug， 然后[第一性原理分析]流程，思考是否有更简单、更稳健的实现。
- 自动化执行:laywright脚本在终端持续会话，禁止无意义的暂停。


### 1.3 默认执行原则
- 能从仓库、配置、代码、文档中直接获得答案时，不向用户重复提问
- 修改前优先复用已有实现、已有模式、已有工具
- 局部任务也必须遵守完整工程要求，不能以“只是小改动”为由跳过规范
- 若发现用户需求可能引发架构、安全、数据一致性或协作流程风险，应主动提示
- 向用户输出结果、风险、未完成项

### 1.4 规则优先级

出现冲突时，按以下优先级从高到低执行：

1. 用户当前明确指令
2. 安全、合规、数据保护相关硬性约束
3. 本 `AGENTS.md`
4. 读取 `.claude/skills/learned/SKILL.md`
5. 判断是否命中某个 Skill；若命中，先读取对应 `SKILL.md`
6. 阅读与任务直接相关的代码、配置、测试、文档
7. 通用工程最佳实践

若低优先级规则与高优先级规则冲突，必须服从高优先级规则；若无法兼容，应明确指出冲突点并采取保守方案。

### 1.5 何时必须先补上下文
以下场景不得直接动手，必须先补齐上下文：

- 需求会改动多个模块，且影响边界不清晰
- 需求涉及数据库 Schema、接口契约、权限模型、缓存策略或异步流程
- 需求与现有实现明显冲突，无法判断应遵循哪一套行为
- 用户描述与代码现状不一致，可能存在历史变更或工作区未提交改动

### 1.6 通用工程规范

#### 1.6.1 代码与结构

- 目录按功能或领域分层组织，遵循关注点分离原则
- 目录和文件命名必须一致、可读、可描述其职责
- 相关功能尽量放在同一模块，减少跨模块耦合
- 避免过深嵌套，一般不超过 3 至 4 层
- 区分代码、资源、配置、测试文件
- 依赖声明集中管理，避免重复定义

#### 1.6.2 设计与实现原则

- 保持单一职责，提升可测试性
- 遵循 DRY，重复逻辑要抽取复用
- 遵循 KISS，优先简单直接的实现
- 命名必须表达真实含义，避免模糊缩写
- 复杂逻辑应补充注释，但避免对自解释代码做无价值注释
- 任何外部输入都应考虑校验、异常处理和失败回退
- 不得提供不安全代码，如 SQL 注入、硬编码密钥、未校验输入、越权访问
- 涉及安全场景时，应遵循 `security-review` Skill
- 涉及大数据量、并发、IO、缓存、远程调用时，应主动评估性能影响
- 不得使用已弃用、过时或与项目版本约束不兼容的技术

---

## 2. 开始任务前的最低检查

1. 阅读 `CLAUDE.md`
2. 阅读 `.claude/skills/learned/SKILL.md`
3. 判断是否命中某个 skill；命中则先读对应 `SKILL.md`
4. 判断任务命中了哪些项目专有规则文档；只要命中，必须先阅读对应文档，未读不得开始实现
5. 确认任务涉及的模块、目录、数据库环境、前后端范围
6. 阅读将被修改的文件与相邻实现
7. 再开始修改、测试与验证

## 3. 文档维护规范

- 优先补充既有文档，不新增零散文档
- 需求、架构、测试、实现说明分层维护，各归其位
- 文档命名、章节结构、术语应与现有文档保持一致
- 同一业务概念在需求、架构、测试、代码中保持同名

### 3.1 需求规格文档规则

- 新需求、子需求、细化条款，统一补充到既有《需求规格说明书》或同类需求文档
- 不得为单个小需求额外新建零散需求文档
- 若需求跨多个模块，应在主需求文档中明确边界，并在相关模块需求文档中建立引用

### 3.2 架构与详细设计文档规则

- 新方案、子方案、实施要点，统一补充到既有《架构与详细设计》文档
- 不得为局部实现新增零碎“实施方案”“设计说明”等重复文档
- 当实现已经改变模块边界、API 组织、数据库设计或关键流程时，必须同步回写对应架构文档

### 3.3 允许新建文档的例外

- 全新一级业务模块，可新建对应需求与架构文档
- 跨模块通用规范，可独立成文，但必须在相关模块文档中引用并保持同步
- 新增测试基线、专项调研、阶段性路线图时，可在对应目录下新增文档，但不得替代主需求/主架构文档

---


## 4.MCP规范

- 操作系统元数据库时，必须使用 MCP `data_service`；
- 联邦查询postgresql业务数据库 `federation` 时，必须使用 MCP `postg_federation1`, 联邦查询mysql业务数据库 `mysql_federation` 时，必须使用 MCP `mysql_federation`


## 5. Skills 使用规范

### 5.1 通用原则

- 处理任何任务前，优先读取 `.claude/skills/learned/SKILL.md`
- 用户明确点名某个 skill，或任务描述明显匹配某个 skill 的 `description` 时，视为命中，必须先读取对应 `SKILL.md`，再执行
- 修改、补充、排查、验证等局部任务同样适用，不仅限于完整功能开发
- 多个 skill 同时命中时，先读最直接相关、约束最强的 skill，再补读其他 skill

任务分类判断原则：

- 文档类任务：先区分“写内容”还是“处理文件格式”
- 界面类任务：先区分“常规前端页面 / 组件”还是“复杂 artifact”
- 视觉类任务：先区分“静态设计”还是“代码生成艺术”

### 5.2 技能触发矩阵

| Skill                   | 应用场景与用途                                                            | 触发规则                                                                                                                                                  |
| ----------------------- | ------------------------------------------------------------------ | ----------------------------------------------------------------------------------------------------------------------------------------------------- |
| `algorithmic-art`       | 通过代码生成原创视觉作品，输出算法说明与可交互 HTML/JS                                    | 用户要求生成艺术、生成式图形、算法艺术、flow fields、particle systems、p5.js 交互艺术时，先读取 `.claude/skills/algorithmic-art/SKILL.md`                                            |
| `brand-guidelines`      | 应用 Anthropic 官方品牌色、字体和视觉规范                                         | 用户明确要求使用 Anthropic 品牌风格、品牌色、官方视觉规范、品牌排版时，先读取 `.claude/skills/brand-guidelines/SKILL.md`                                                               |
| `canvas-design`         | 创建海报、静态视觉稿、PDF/PNG 设计成品                                            | 用户要求创建海报、单张视觉稿、静态艺术图、PDF/PNG 设计成品时，先读取 `.claude/skills/canvas-design/SKILL.md`；若要求浏览器可运行的生成式艺术，则改用 `algorithmic-art`                                  |
| `doc-coauthoring`       | 用于需求文档、技术方案、PRD、RFC、决策文档、提案等内容共创                                   | 用户要求撰写需求文档、技术方案、设计文档、PRD、RFC、决策文档、提案、说明书，或明显进入“共同起草文档”流程时，先读取 `.claude/skills/doc-coauthoring/SKILL.md`                                               |
| `doc-governance`        | 项目文档治理规则，用于 `docs` 目录文档的新增、修改、迁移、重命名与索引维护                          | 任务涉及 `docs/**/*.md` 的新增、修改、迁移、重命名、目录 `README.md` 索引维护、文档路径整理、`docs/assets` 资源归档时，先读取 `.claude/skills/doc-governance/SKILL.md`                              |
| `docx`                  | 创建、修改、分析 `.docx` 文件                                                | 任务涉及创建、修改、分析 `.docx` 文件时，先读取 `.claude/skills/docx/SKILL.md`                                                                                           |
| `frontend-design`       | 项目内前端界面开发默认 Skill，用于页面、组件、网站、落地页、Dashboard、HTML/CSS 布局与 UI 美化      | 用户要求编写或修改前端界面、页面、网站、落地页、Dashboard、React 组件、HTML/CSS 布局，或要求美化已有 Web UI 时，先读取 `.claude/skills/frontend-design/SKILL.md`；只要涉及当前前端工程页面、组件、样式、交互视觉修改，均视为命中 |
| `internal-comms`        | 撰写内部沟通材料                                                           | 用户要求撰写内部沟通材料时，先读取 `.claude/skills/internal-comms/SKILL.md`                                                                                            |
| `mcp-builder`           | 创建、修改、接入、配置 MCP Server，设计 MCP tools                                | 用户要求创建或配置 MCP Server、设计 MCP tools、封装外部服务为 MCP 能力时，先读取 `.claude/skills/mcp-builder/SKILL.md`                                                           |
| `pdf`                   | 填写 PDF 表单、提取文本表格、拆分合并、批量处理、生成或分析 PDF                               | 任务涉及 PDF 表单、文本/表格提取、拆分合并、批量处理、生成或分析 PDF 时，先读取 `.claude/skills/pdf/SKILL.md`                                                                           |
| `pptx`                  | 创建、修改、分析 `.pptx` 演示文稿                                              | 任务涉及创建、修改、分析 `.pptx` 文件时，先读取 `.claude/skills/pptx/SKILL.md`                                                                                           |
| `react-hook-form`       | 项目前端复杂表单开发规则，用于 React Hook Form、Zod、字段联动与自定义表单组件集成                | 在 `linkinsight-web` 中处理复杂表单、跨组件表单状态、类型安全校验、字段联动、动态字段、与自定义输入组件集成时，先读取 `.claude/skills/react-hook-form/SKILL.md`；简单表单不强制触发                   |
| `skill-creator`         | 新建或更新 Skill，设计 Skill 结构                                            | 用户要求新建 Skill、更新 Skill、设计 Skill 结构、封装工作流为 Skill 时，先读取 `.claude/skills/skill-creator/SKILL.md`                                                          |
| `slack-gif-creator`     | 创建用于 Slack 的动图并满足 Slack 约束                                         | 用户要求制作用于 Slack 的 GIF，或明确要求满足 Slack GIF 限制时，先读取 `.claude/skills/slack-gif-creator/SKILL.md`                                                            |
| `theme-factory`         | 给文档、幻灯片、HTML 页面、报告、落地页等已有成品统一换主题                                   | 用户要求统一换主题、套色、换字体、应用视觉主题时，先读取 `.claude/skills/theme-factory/SKILL.md`；若是设计界面本身，优先 `frontend-design`                                                    |
| `typescript`            | 项目 TypeScript 编码规则，用于 `linkinsight-web` 或其他 TypeScript 代码开发                | 任务涉及 `*.ts`、`*.tsx`、`*.d.ts` 的新增、修改、重构、类型报错修复、类型建模、工具类型、类型守卫或前端脚本开发时，先读取 `.claude/skills/typescript/SKILL.md`                               |
| `web-artifacts-builder` | 创建复杂 HTML artifact，适用于 React、Tailwind、shadcn/ui、多组件结构、状态管理、路由、打包流程 | 用户要求创建复杂 HTML artifact，且需要多组件结构、状态管理、路由或打包流程时，先读取 `.claude/skills/web-artifacts-builder/SKILL.md`；简单单页 HTML/JSX 或普通项目页面仍优先 `frontend-design`          |
| `webapp-testing`        | 使用 Playwright 进行本地 Web 应用测试与页面验证                                   | 任务涉及本地 Web 应用测试、Playwright 自动化、页面交互验证、截图取证、浏览器日志排查时，先读取 `.claude/skills/webapp-testing/SKILL.md`；编写测试代码、验证前端改动、复现 UI 问题时均触发                           |
| `xlsx`                  | 创建、修改、分析 `.xlsx/.xlsm/.csv/.tsv` 等表格文件                             | 任务涉及创建、修改、分析表格文件时，先读取 `.claude/skills/xlsx/SKILL.md`              


### 5.3 组合触发规则

- 前端开发后做页面验证：先读 `frontend-design`，再读 `webapp-testing`
- 编写或整理 `docs` 目录文档内容时：先按任务需要读 `doc-coauthoring`，涉及文档迁移、重命名、索引维护、资源归档时再补读 `doc-governance`
- 撰写方案文档并同时产出 Word / PDF / PPT：先读 `doc-coauthoring`，再读 `docx` / `pdf` / `pptx`
- 修改已有 HTML / 幻灯片 / 报告的视觉主题：先读 `theme-factory`，必要时补读 `frontend-design`
- 前端页面含复杂表单开发时：先读 `frontend-design`，复杂表单再读 `react-hook-form`；若涉及 TypeScript 类型建模、报错修复或 `ts/tsx` 改动，再补读 `typescript`
- 制作静态海报 / 画布作品并导出 PNG / PDF：先读 `canvas-design`；若要求代码驱动生成与可交互参数控制，则改用 `algorithmic-art`

### 5.4 learned skill 维护规则

- 当用户明确指出当前做法不合适，并给出可复用、可泛化的新要求时，应将该规范追加写入 `.claude/skills/learned/SKILL.md`
- learned skill 只记录项目内反复适用、可稳定复用的明确规则
- 临时偏好、一次性例外、不稳定约定，不写入 learned skill


---

## 6. 项目专有规则文档入口与强制读取规则


## 6. 项目专有规则文档入口与强制读取规则

以下文档不是可选参考资料，而是对应任务的强制执行规范。
只要任务命中对应范围，必须先阅读对应文档，再开始分析、修改、测试与验证。
不得以“改动很小”“只是顺手修一下”“之前看过”为由跳过。
若一个任务同时命中多个类别，必须把所有命中文档都读完后再动手。

### 6.1 通用架构与选型

- 涉及工程结构、模块边界、前后端映射、API 路径、环境配置、MCP 使用时：
  必须先阅读 `@docs/research/common/工程架构说明.md`

- 涉及技术选型、依赖引入、框架能力边界、版本兼容性判断时：
  必须先阅读 `@docs/research/common/技术选型说明书.md`

### 6.2 后端任务

- 涉及后端编码、分层职责、DTO/VO/Entity、配置绑定、Spring Boot 约定、Java 21 特性使用边界时：
  必须先阅读 `@docs/research/common/后端开发规范.md`

### 6.3 前端任务

- 只要任务涉及前端页面、组件、样式、布局、交互、主题、表单、表格、抽屉、弹窗、Dashboard、可访问性、前端重构或 UI 优化：
  必须先阅读 `@docs/research/common/UI设计规范.md`
  必须先阅读 `@docs/research/common/前端开发规范.md`

- 前端任务中：
  `UI设计规范.md` 负责约束视觉、布局、组件形态与交互一致性；
  `前端开发规范.md` 负责约束实现方式、目录组织、组件拆分、状态管理、接口接入与工程写法。
  两者都命中时，不得只读其中一个。

### 6.4 数据库任务

- 涉及数据库命名、Schema、字段设计、多租户、MyBatis、Flyway、Repository 映射、SQL 编写与数据一致性时：
  必须先阅读 `@docs/research/common/数据库设计规约.md`

### 6.5 测试任务

- 涉及单元测试、集成测试、E2E、覆盖率、测试分层、命名规范、门禁要求时：
  必须先阅读 `@docs/research/common/测试规范.md`
- 不要使用容器，所有测试必须在本地运行。  

### 6.6 Git 与协作

- 涉及分支策略、提交规范、PR、代码评审、变更组织与协作流程时：
  必须先阅读 `@docs/research/common/Git与协作规范.md`
